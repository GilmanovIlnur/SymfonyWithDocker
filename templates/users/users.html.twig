{% extends 'base.html.twig' %}

{% block body %}
    <h1>
        Все пользователи
    </h1>
    {% for user in users %}
        {{ loop.index }}.
        <a href="profile/{{ user.id }}">{{ user.username }}</a>
        <br/>
    {% endfor %}
{% endblock %}